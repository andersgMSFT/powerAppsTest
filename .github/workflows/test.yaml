name: Test run package flow

on: [workflow_dispatch]

jobs:
  runPackage: 
    runs-on: ubuntu-latest 
    steps:
      - uses: actions/checkout@v3
      - name: run action 
        uses: ../actions/package.yaml@main

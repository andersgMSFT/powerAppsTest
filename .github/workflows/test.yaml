name: Test run package flow

on: [workflow_dispatch]

env: 
  solutionPath: BcSampleAppsSolution
  solutionPath: BcSampleAppsSolution
  solutionVersion: "1.0.4"

jobs:
  runPackage:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: run action
        uses: ./.github/actions/packageSolution
